#ifndef MY_GLOBALS_H
#define MY_GLOBALS_H

using namespace std;

enum types {BUY, SELL, NONE_TYPE};
enum states {PENDING, DONE, CANCELLED, NONE_STATE};
enum actions {ADD, CANCEL, NONE_ACTION};
enum order_err_codes {ERR_SIZE, ERR_STATUS, SUCCESS};

// extern types;
// extern states;
// extern actions;

#endif