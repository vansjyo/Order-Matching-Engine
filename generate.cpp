#include <bits/stdc++.h>

using namespace std;

int main() 
{
    int numberOfSamples = 1000;
    while ( numberOfSamples )
    {
        ofstream sampleFile;
        sampleFile.open ( "order_format", ios::app|ios::out );
        numberOfSamples--;
    }
    return 0;
}